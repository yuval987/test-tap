apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: helloworld-go-2
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: nginx
          env:
            - name: TARGET
              value: "Go Sample v1"
